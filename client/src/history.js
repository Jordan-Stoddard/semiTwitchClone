                        // This is a path to the react-browser-router library that references a function called createBrowserHistory
// We're setting createHistory = createBrowserHistory() essentially 
import createHistory from 'history/createBrowserHistory'

// we're exporting the method.
export default createHistory()